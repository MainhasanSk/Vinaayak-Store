import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.vinayakstore.app',
    appName: 'Vinayak Store',
    webDir: 'dist',
    bundledWebRuntime: false
};

export default config;
